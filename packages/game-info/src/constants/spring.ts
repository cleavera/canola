import { AFTERNOON, EARLY_HOURS, EVENING, MIDNIGHT, MORNING, NOON } from './times-of-day';

export const Spring = {
    NAME: 'Spring',
    TICKS: [
        MIDNIGHT,
        EARLY_HOURS,
        MORNING,
        NOON,
        AFTERNOON,
        EVENING
    ]
};
